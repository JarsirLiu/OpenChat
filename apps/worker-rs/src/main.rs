#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing::info!(
        "openchat-worker placeholder started; task scheduling and outbound channel delivery will live here"
    );
    Ok(())
}
