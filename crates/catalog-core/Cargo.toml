[package]
name = "openchat-catalog-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "openchat_catalog_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
openchat-core = { path = "../openchat-core" }
